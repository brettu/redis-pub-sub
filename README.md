Some pub/sub Redis applications. I love Redis and it's compliment to Ruby programming.

There is only one application here so far. In one terminal window run

`$ ruby publish.rb`

and in another window run

`$ ruby subscribe.rb`

